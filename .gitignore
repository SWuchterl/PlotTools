Datacards_*
FitStudies_*
BestOrder_raw/
*.log
*.root
*.hdf5
*.cc
*.out
*Templates*
*.pdf
*.png

# rabbit pipeline generated reports (tensors/plots already covered above;
# configs/*.csv are real inputs, these suffixes never collide with them)
*_decisions.csv
*_symmetrisation.csv
*_report.json
*_summary.npz

# HTCondor submission artifacts (submit_toys_condor.sh output: generated
# executable/submit file + per-job stdout/stderr/log, not source)
condor_*/

# python
__pycache__/
*.pyc

# rabbit venv (see setup_rabbit_env.sh to rebuild)
rabbit_env/

# nested independent repo, not vendored here
datacards/

# agent/AI tooling config, not project docs
.claude/
.agents/
CLAUDE.md
AGENTS.md
